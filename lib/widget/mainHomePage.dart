import 'package:flutter/material.dart';

class mainHome extends StatelessWidget {
  @override
  Widget build(BuildContext context)
  {
    return Scaffold(body: Center(child: Text("My principal home")));
  }
}